# Anki Card Generator — dependencies
# Install with: pip install -r requirements.txt
#
# ffmpeg is also required for the "Local video" feature (video_gen.py) but is
# a system tool, not a Python package:
#   Windows: winget install ffmpeg
#   macOS:   brew install ffmpeg
#   Linux:   sudo apt install ffmpeg

# Web server (server.py + index.html)
flask>=3.1
flask-cors>=6.0
werkzeug>=3.1

# AI providers
google-genai>=0.3        # Gemini Flash / Gemini Pro
groq>=0.11                # Groq Llama + Whisper transcription (also used by format_pass.py)
openai>=1.0                # OpenRouter and NVIDIA clients (OpenAI-compatible APIs)

# Content extraction
youtube-transcript-api>=1.2   # YouTube auto-transcript + proxy_config support
                               # (0.6.x used a different, static-method API —
                               # this codebase already relies on the 1.x
                               # instance-based YouTubeTranscriptApi().fetch())
pdfplumber>=0.11               # PDF text extraction
